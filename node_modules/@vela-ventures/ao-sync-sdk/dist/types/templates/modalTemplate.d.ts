export interface ModalTemplateProps {
    subTitle: string;
    description?: string;
    qrCodeData?: string;
    animationData?: any;
    autoClose?: boolean;
    walletClient?: any;
}
export declare const createModalTemplate: ({ subTitle, description, qrCodeData, animationData, autoClose, walletClient, }: ModalTemplateProps) => HTMLDivElement;
export declare function connectionModalMessage(modalMessage: "success" | "fail"): void;
